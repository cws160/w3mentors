/** @deprecated Route styles are handled by RouteStyleSync — kept for compatibility. */
export function useMarketingStyles(_enabled: boolean) {
  // No-op: applyMarketingStylesheets() runs in RouteStyleSync via useLayoutEffect.
}
