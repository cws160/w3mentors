/** @deprecated Route styles are handled by RouteStyleSync — kept for compatibility. */
export function useDashboardStyles(_enabled: boolean) {
  // No-op: applyDashboardStylesheets() runs in RouteStyleSync via useLayoutEffect.
}
